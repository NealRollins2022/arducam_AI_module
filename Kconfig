menuconfig ARDUCAM
    bool "Enable ArduCAM AI module support"
    default y
    help
      Enable support for the ArduCAM SPI Camera module.

if ARDUCAM

config ARDUCAM_DEBUG
    bool "Enable debug logging for ArduCAM"
    default n
    help
      Enable verbose debug output for the ArduCAM driver.

endif # ARDUCAM